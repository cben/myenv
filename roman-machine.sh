#!/bin/bash

# Partial brain dump of things installed for dad...
# skype, dropbox

# Important: LibreOffice gets very stuck on unknown codecs,
# and eventually treats the video as audio.
# Installing codecs (ubuntu-restricted-extras) helps!

sudo apt install ubuntu-restricted-extras libreoffice calligra vlc
